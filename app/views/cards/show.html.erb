<div class="row">
  <div class="small-12 large-5 columns">
    <h1>Cards</h1>
    <h2><%= @card.category.name %></h2>
    <h3><%= @card.name %></h3>
    <div>
      <%= link_to "Download PDF", :action => "show", :format => :pdf, data: {turbolinks: false} %>
    </div>
    <div>
      <%= link_to "Edit card", :action => "edit" %>
    </div>
  </div>
  
  <%= render 'card' %>
</div>
